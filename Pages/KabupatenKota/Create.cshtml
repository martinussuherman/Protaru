@page
@model CreateModel

@{
    Layout = "_CommonLayout";
    ViewData["Title"] = "Tambah Kabupaten/Kota";
    ViewData["PageTitle"] = "Kabupaten/Kota";
}

<div class="card-body">
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label class="control-label">Nama</label>
            <input asp-for="KabupatenKota.Nama" class="form-control" />
            <span asp-validation-for="KabupatenKota.Nama" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label class="control-label">Provinsi</label>
            <select asp-for="KabupatenKota.KodeProvinsi" class ="form-control" asp-items="ViewBag.Provinsi"></select>
        </div>

        <input type="submit" value="Simpan" class="btn btn-primary" />
        <a asp-page="./Index" class="btn btn-warning">Kembali</a>
    </form>
</div>

@section Scripts 
{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
