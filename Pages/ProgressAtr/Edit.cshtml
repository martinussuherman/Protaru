@page "{id:int?}"
@model EditModel

@{
    Layout = "_CommonLayout";
    ViewData["Title"] = "Ubah Progress RTR";
    ViewData["PageTitle"] = "Progress RTR";
}

<div class="card-body">
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="ProgressRtr.Kode" />

        <div class="form-group">
            <label class="control-label">Nama</label>
            <input asp-for="ProgressRtr.Nama" class="form-control" />
            <span asp-validation-for="ProgressRtr.Nama" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label class="control-label">Nomor</label>
            <input asp-for="ProgressRtr.Nomor" class="form-control" />
            <span asp-validation-for="ProgressRtr.Nomor" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label class="control-label">Jenis RTR</label>
            <select asp-for="ProgressRtr.KodeJenisAtr" class ="form-control" asp-items="ViewBag.JenisRtr"></select>
        </div>

        <input type="submit" value="Simpan" class="btn btn-primary" />
        <a asp-page="./Index" class="btn btn-warning">Kembali</a>
    </form>
</div>

@section Scripts 
{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
