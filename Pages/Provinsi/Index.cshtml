@page
@model MonevAtr.Pages.Provinsi.IndexModel

@{
    ViewData["Title"] = "Daftar Provinsi";
}

<h1>Daftar Provinsi</h1>

<p>
    <a asp-page="Create">Tambah Provinsi</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Provinsi[0].Kode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Provinsi[0].Nama)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Provinsi) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Kode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nama)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Kode">Ubah</a> |
                <a asp-page="./Delete" asp-route-id="@item.Kode">Hapus</a>
            </td>
        </tr>
}
    </tbody>
</table>
